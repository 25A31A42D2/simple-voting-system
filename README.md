# simple-voting-system
A simple C-based voting system where users choose a candidate from a menu. Votes are counted using variables, and the program displays total votes and the winner when voting ends. Uses loops, switch-case, and basic input/output.
